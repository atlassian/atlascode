function bad() {
    x = 4;
    y = 2;
}
function add(a, b) {
    result = a + b; //no space after hash,bad style
    return result; //   too many spaces, inconsistent
}

function multiply() {
    function test() {}
    x = 4;
    y = 23;
    return x * y; //this comment has no capital and no period and is very long and goes on and on forever without stopping
}
