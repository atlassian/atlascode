import { AlertTitle } from '@mui/material';
import React from 'react';

export const MissingScopeBanner: React.FC = () => {
    return <AlertTitle color={'red'}>Missing Scopes Banner</AlertTitle>;
};
